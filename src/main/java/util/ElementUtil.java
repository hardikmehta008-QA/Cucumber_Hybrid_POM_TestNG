package util;

// Utility class intended to hold common element interactions (click, type, wait, scroll).
// Currently a placeholder — implement reusable helpers here to reduce duplication across page objects.
public class ElementUtil {
    // Example helper signatures to consider adding:
    // public static void clickWhenReady(WebDriver driver, By locator, Duration timeout) { ... }
    // public static void type(WebDriver driver, By locator, String text) { ... }
    // public static String getText(WebDriver driver, By locator) { ... }
}
